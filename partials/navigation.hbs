<div class="nav">
    <a href="#" class="nav-close">
        <span class="hidden">Close</span>
    </a>
    <ul class="pure-menu-list">
        {{#foreach navigation}}
            <li class="pure-menu-item nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}" class="pure-menu-link">{{label}}</a></li>
        {{/foreach}}
    </ul>
</div>
<span class="nav-cover"></span>
